# AGENTS.md — AI Agent 集成指南

51job-cli 是纯 CLI，每条命令输出结构化纯文本（`--json` 可选），适合 LLM 通过子进程编排招聘流水线。

> **🧭 本仓库是 CLI 源码仓（原子能力）。要跑「一条龙招聘流水线」，请去
> `skills/recruit-daily-51job/`（在 recruiting-copilot 仓库）——那是外层编排，
> 管「查未读→寻源→初筛→打招呼→台账→日报」的完整工作流；本文件只管命令怎么用。

## 运行与输出约定

- **纯 JSON 单文档协议**：以 `--json` 唤起时，`stdout` 被强制并承诺仅会收到单组最终结果被 `JSON.parse` 接受返回的唯一完整文档字典。任何命令跑出的额外挂载状态（即便是执行过程内产生的详情），乃至 `hiResult` 、`error` 也统统都会在跑完时一并融入单节点再输出。执行过程日志在 `--json` 下全部重定向改走 `stderr` 输出 `[info]` 流格式，避免强污染抓取端点。
- **退出码系统分级 (Exit Code System)**：
  - `exit 0`：操作符合安全预期完整通过无报错拦截（*包括明确不该执行的场景，如 `--dry-run` 探测执行跳出，或者在 `action` 非安全选项下的 Y/N 被用户明确点 `no` 取消执行*）。
  - `exit 1`：业务环境功能级拦截抛错返回（Hi聊余量扣打耗尽、详情找寻未定位命中目标、点击送达检查无效、`search` 面板没有可用职位数据项）。会随带提示词写出给人工干预/下一轮 LLM 逻辑调整参考。
  - `exit 2`：底层可用性（`availability` 防崩溃门禁机制）检测强制锁区。这种场景不是代码异常，表示远端 51job eHire 更迭了基础组件导致本地脚本不再稳定吻合验证区。这需要触发更新策略捕获新的前端底座快照。

## 安全红线（Agent 必须遵守）

1. **不可逆动作非交互限制**：`action` 接受、拒绝类不可逆状态流转、`greet` 打招呼操作是严肃的决策。在不可挂起进行人机交互（如 stdin 进程为管道环境或遭主动关闭（EOF））时，确认默认项总是会被强制**安全拒绝断开挂起并阻断**。若是纯静默全自动环境必须用 `--no-confirm` 命令透传明示放行风险。
2. **幂等护栏与勿盲发执行**：系统底座具有一定的在网络抖动和加载异步情况下的事件重刷重发挽救支持。**但是绝不兜底覆盖重发写流操作**。在捕捉到 `send` 或者响应判定超时（`failed`/`unknown` 时），极大概率它已经发出到 51job 但是 IM SDK 未显示重置完成框！此时调用代码绝对不应该使用轮询重新再执行一次本操作项（同一段话外发二次），而应该记为未知失败并走巡检补偿查询确认后交给人定判断，或者进入 `unknown_skipped` 日志库。
3. **不要并发批量操作**。所有写操作之间有随机节流（800–2500ms），外层脚本不要再叠加高频循环。
4. **风控熔断优先**。命令检测到风控/验证页面会立即停止并输出 `风控熔断` 警告（在断网引起的无伤网络超时时也只有两分钟屏蔽跳过验证期）。此时**不要重试**。
5. **禁止无头模式**。无头模式会被平台风控识别（UA 会与 Client Hints 矛盾），有封号风险。默认有头，勿改环境变量覆盖（`RECRUIT_BROWSER_HEADLESS=false` 保底）。
6. **数据与资料环境敏感**。Cookie 仅存于 `~/.51job-cli/.cache/`，日常用 `51job clean` 时清理历史残留文本；不要读取提取本安全库中的 `session.lock` 和持久化凭证文件。

## 典型招聘流程编排

```
1. 51job login                  → 首次使用：用户扫码登录（一次性，立即返回不强制长阻塞）
2. 51job wait-login             → 探测判断登录操作闭环，登入就绪
3. 51job list --unread --json   → 获取未读候选人（解析 JSON）
4. 51job chat <姓名> --index N   → 打开会话（同名用序号，确保传 index 跟抓出来的 list index 索引匹配一致使用，支持 --unread 按相同条件透滤）
5. 51job action resume          → 索要简历
6. 51job send --text "明细文本"
7. 51job search <关键词> --json → 人才搜索（13 维筛选取值见 skills 下字典库关联通道）
8. 51job greet <姓名> --job <岗> → 对候选人打招呼（补搜池后再确认，带上 --no-confirm 允许 AI 做最后裁决）
9. 51job recommend <岗位> --json → 人才望远镜推荐（姓名·点名）
10. 51job inspect <姓名> --json  → 候选详情结构化（兼带检查 --hi 会在拿到后尝试一次并入返回包协议）
11. 51job positions --json      → 职位列表目录和库
12. 51job positions --candidates <职位名> --source search --json → 投递不足时人才池搜索扩充该职位候选（自动注入该职位城市/学历筛选；--json 返回 {position, source, count, candidates[]}）
13. 51job talent-detail <姓名> --json → 投递/聊天来源候选人详情（--hi 走免费「回复」）；talent-detail 与 inspect 二选一，按候选人来源决定（人才管理页 vs 搜索池）
```

> 📌 语义区分（勿混）：`list`=工作台投递箱全职位聚合流；`positions --candidates`=按职位的投递/搜索候选人；`search`=主动人才池搜索；`talent-detail`=投递/聊天来源详情；`inspect`=搜索池来源详情。搜索匹配的「匹配人才」≠「主动投递者」，Hi 前注意成本与意图。

## 环境变量指南

| 变量 | 作用 | 默认 |
|------|------|------|
| `51JOB_PROJECT_ENV` | 打开防注入验证，设为 =1 才会加载调用执行点当前工作包目录下的 `./.env` 本地测试或重载环境级属性 | 0 (禁止) |
| `RECRUIT_BROWSER_HEADLESS` 或 `51JOB_BROWSER_HEADLESS` | true=无头（不推荐使用） | false |
| `51JOB_DELAY` | 节流毫秒间隙逗号组合，建议带入如 `800,2500` 形成正态波动 | `800,2500` |
| `51JOB_BAIDU_API_KEY`/`SECRET_KEY` | 对在线预览履历等图形材料申请图片打码处理成明文配置用，优先读取 | - |
| `51JOB_RESUME_OCR` | 控制截取留存之后数据跨境同意确认使用 OCR 返回。配置=1则放行。 | 0 (即关闭，留存图片) |
| `51JOB_BLOCK_REPORT_PATTERNS` | 安全脚本/全量埋点的 HTTP CDP 屏蔽级别配置集（逗号拼接路由格式组），否则以静默 log 打印不做强过滤抹杀，因为防止误杀阻断相关合法逻辑组件响应。（详请走查本地 debug/probe） | - |
